1) View current database instance name:
	select name from v$database;

2) List all tables owned by your current user account:
	select table_name from user_tables;

3) View the column structure of a specific table:
	describe table_name;

4) List tables across the entire system (requires admin):
	SELECT owner, table_name FROM dba_tables;

